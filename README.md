# File introduction
-   `cal.sh`
    main program. caculate the distribution in meshgrid.
-   `plot.sh (x) (y)`
    plot (x,y) detector energy spectrum.
-   `buildlib.sh` 
    build before run
# request

-   ROOT
-   ExRootAnalysis
-   Pandas
-   Matplotlib
-   Numpy
